{
  "name": "coedu",
  "version": "0.0.1",
  "description": "Simple app that incorporates git and github into classroms!",
  "repository": "rista404/coedu",
  "main": "src/main.js",
  "scripts": {
    "start": "parallelshell \"npm run dev\" \"npm run app\"",
    "app": "electron src/main.js",
    "dev": "npm run dev:jade; npm run dev:sass; npm run dev:js;",
    "dev:jade": "jade --pretty -w src/index.jade -o dist",
    "dev:js": "watchify src/app.js -v -o dist/js/app.min.js -t [ babelify [ --presets [ es2015 react ] ]",
    "dev:sass": "node-sass -i -w src/styles/main.sass -o dist/css ",
    "clean": "rm -rf dist/css dist/js dist/index.html",
    "build": "npm run clean; mkdir dist/css dist/js; npm run build:jade; npm run build:sass;",
    "build:jade": "jade src -o dist",
    "build:js": "browserify -t [ babelify --presets [ es2015 react ] ] src/app.js dist/js/app.min.js",
    "build:sass": "node-sass -i --output-style compressed src/styles/main.sass | postcss --use autoprefixer -o dist/css/main.css"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^6.3.3",
    "babel-preset-es2015": "^6.5.0",
    "jade": "^1.11.0",
    "node-sass": "^3.4.2",
    "parallelshell": "^2.0.0",
    "postcss-cli": "^2.5.1",
    "watchify": "^3.7.0",
    "babel-preset-react": "^6.5.0",
    "babelify": "^7.2.0"
  },
  "dependencies": {
    "electron-prebuilt": "^0.36.7",
    "react": "^0.14.7",
    "react-dom": "^0.14.7"
  }
}
